import { SlideLayout, Notes } from 'spectacle'
import { CodeDisplay, File, Tests } from '../../components/CodeDisplayWithSlideTracking'

<SlideLayout.Full>
## Exercise: Style a Habit Tracker Card

<CodeDisplay >
  <File name="index.html">
```html
<!DOCTYPE html>
<html>
<head>
    <title>Habit Card Styling Exercise</title>
</head>
<body>
    <h2>Style this Habit Card</h2>
    
    <!-- TODO: Style this habit card to look professional and engaging -->
    <div class="habit-card">
        <div class="habit-header">
            <h3 class="habit-title">Morning Meditation</h3>
            <span class="habit-category">Wellness</span>
        </div>
        
        <div class="habit-body">
            <div class="streak-info">
                <span class="streak-number">15</span>
                <span class="streak-label">day streak</span>
            </div>
            
            <div class="progress-bar">
                <div class="progress-fill" style="width: 75%"></div>
            </div>
            
            <p class="habit-time">Best time: 6:00 AM</p>
        </div>
        
        <div class="habit-footer">
            <button class="btn-complete">Mark Complete</button>
            <button class="btn-skip">Skip Today</button>
        </div>
    </div>
</body>
</html>
```
  </File>
  <File name="index.css">
```css
/* TODO: Add your styles here to make the habit card look great!
   
   Requirements:
   1. Card should have rounded corners and a subtle shadow
   2. Use flexbox or grid for layout
   3. Category should be styled as a badge/tag
   4. Progress bar should be visual and animated
   5. Buttons should have hover effects
   6. Use a color scheme (primary: #4CAF50, secondary: #2196F3)
*/

.habit-card {
    /* Your styles here */
}

// ---

/* Solution 1: Basic styling with all requirements */
* {
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    background-color: #f5f5f5;
    padding: 20px;
}

.habit-card {
    background-color: white;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    padding: 24px;
    max-width: 350px;
    margin: 0 auto;
}

.habit-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
}

.habit-title {
    margin: 0;
    color: #333;
    font-size: 20px;
}

.habit-category {
    background-color: #2196F3;
    color: white;
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: bold;
}

.habit-body {
    margin-bottom: 24px;
}

.streak-info {
    display: flex;
    align-items: baseline;
    gap: 8px;
    margin-bottom: 16px;
}

.streak-number {
    font-size: 48px;
    font-weight: bold;
    color: #4CAF50;
}

.streak-label {
    color: #666;
    font-size: 16px;
}

.progress-bar {
    height: 8px;
    background-color: #e0e0e0;
    border-radius: 4px;
    overflow: hidden;
    margin-bottom: 12px;
}

.progress-fill {
    height: 100%;
    background-color: #4CAF50;
    transition: width 0.3s ease;
}

.habit-time {
    color: #666;
    font-size: 14px;
    margin: 0;
}

.habit-footer {
    display: flex;
    gap: 12px;
}

.btn-complete, .btn-skip {
    flex: 1;
    padding: 10px 16px;
    border: none;
    border-radius: 6px;
    font-size: 14px;
    font-weight: bold;
    cursor: pointer;
    transition: all 0.2s ease;
}

.btn-complete {
    background-color: #4CAF50;
    color: white;
}

.btn-complete:hover {
    background-color: #45a049;
    transform: translateY(-1px);
    box-shadow: 0 2px 4px rgba(76, 175, 80, 0.3);
}

.btn-skip {
    background-color: #f0f0f0;
    color: #666;
}

.btn-skip:hover {
    background-color: #e0e0e0;
}

// ---

/* Solution 2: Enhanced with animations and advanced styling */
* {
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: linear-gradient(135deg, #e3f2fd 0%, #f5f5f5 100%);
    padding: 40px 20px;
    min-height: 100vh;
}

h2 {
    text-align: center;
    color: #333;
    margin-bottom: 40px;
}

.habit-card {
    background: white;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    padding: 28px;
    max-width: 380px;
    margin: 0 auto;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.habit-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
}

.habit-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 24px;
}

.habit-title {
    margin: 0;
    color: #1a1a1a;
    font-size: 22px;
    font-weight: 600;
}

.habit-category {
    background: linear-gradient(135deg, #2196F3, #1976D2);
    color: white;
    padding: 6px 16px;
    border-radius: 24px;
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    box-shadow: 0 2px 8px rgba(33, 150, 243, 0.3);
}

.habit-body {
    margin-bottom: 28px;
}

.streak-info {
    display: flex;
    align-items: baseline;
    gap: 8px;
    margin-bottom: 20px;
    position: relative;
}

.streak-number {
    font-size: 56px;
    font-weight: 700;
    background: linear-gradient(135deg, #4CAF50, #45a049);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
}

.streak-label {
    color: #757575;
    font-size: 16px;
    font-weight: 500;
}

.progress-bar {
    height: 10px;
    background-color: #f0f0f0;
    border-radius: 5px;
    overflow: hidden;
    margin-bottom: 16px;
    position: relative;
}

.progress-fill {
    height: 100%;
    background: linear-gradient(90deg, #4CAF50, #66BB6A);
    border-radius: 5px;
    position: relative;
    animation: progressAnimation 1s ease-out;
}

@keyframes progressAnimation {
    from {
        width: 0 !important;
    }
}

.progress-fill::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
        90deg,
        transparent,
        rgba(255, 255, 255, 0.3),
        transparent
    );
    animation: shimmer 2s infinite;
}

@keyframes shimmer {
    0% {
        transform: translateX(-100%);
    }
    100% {
        transform: translateX(100%);
    }
}

.habit-time {
    color: #616161;
    font-size: 14px;
    margin: 0;
    display: flex;
    align-items: center;
    gap: 8px;
}

.habit-time::before {
    content: '🕐';
    font-size: 16px;
}

.habit-footer {
    display: flex;
    gap: 12px;
}

.btn-complete, .btn-skip {
    flex: 1;
    padding: 12px 20px;
    border: none;
    border-radius: 8px;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
    position: relative;
    overflow: hidden;
}

.btn-complete {
    background: linear-gradient(135deg, #4CAF50, #45a049);
    color: white;
    box-shadow: 0 2px 8px rgba(76, 175, 80, 0.3);
}

.btn-complete:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(76, 175, 80, 0.4);
}

.btn-complete:active {
    transform: translateY(0);
}

.btn-skip {
    background-color: #fafafa;
    color: #757575;
    border: 1px solid #e0e0e0;
}

.btn-skip:hover {
    background-color: #f5f5f5;
    border-color: #bdbdbd;
}

/* Ripple effect for buttons */
.btn-complete::before,
.btn-skip::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.5);
    transform: translate(-50%, -50%);
    transition: width 0.6s, height 0.6s;
}

.btn-complete:active::before,
.btn-skip:active::before {
    width: 300px;
    height: 300px;
}
```
  </File>
  <Tests>
    
```javascript
describe('Habit Card Styling', () => {
    const card = document.querySelector('.habit-card');
    const styles = window.getComputedStyle(card);
    
    it('should have rounded corners', () => {
        expect(styles.borderRadius).to.not.equal('0px');
    });
    
    it('should have a shadow', () => {
        expect(styles.boxShadow).to.not.equal('none');
    });
    
    it('should use flexbox or grid', () => {
        const display = styles.display;
        expect(['flex', 'grid'].includes(display)).to.be.true;
    });
    
    it('should style the complete button with primary color', () => {
        const btn = document.querySelector('.btn-complete');
        const btnStyles = window.getComputedStyle(btn);
        expect(btnStyles.backgroundColor).to.include('76, 175, 80'); // RGB for #4CAF50
    });
});
```
    
  </Tests>
</CodeDisplay>
<Notes>
This comprehensive exercise brings together everything students have learned about CSS. They start with semantic HTML structure and must create a polished, professional habit card component. This mirrors real-world development where you receive a design and must implement it with clean, maintainable CSS.

The exercise teaches component thinking - creating reusable styles that could work for any habit card, not just this specific example. Students must consider the visual hierarchy: the title should draw attention, the category provides context, stats show progress, and the action button should be prominent.

Key challenges include: using Flexbox or Grid for the layout, creating a cohesive color scheme, adding subtle shadows for depth, ensuring proper spacing throughout, making it responsive for different screen sizes, and adding hover states for interactivity. Students should also consider accessibility: sufficient color contrast, focus states for keyboard navigation, and semantic HTML.

This exercise works best when students share their solutions and discuss different approaches. There's no single "correct" solution - different designs can all be effective. The goal is to create something that looks professional, works well, and demonstrates understanding of CSS principles.
</Notes>
</SlideLayout.Full>